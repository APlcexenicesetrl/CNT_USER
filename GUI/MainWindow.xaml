<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="GUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GUI"
    xmlns:Pages="using:GUI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="CNT.USER">
    <Window.SystemBackdrop>
        <MicaBackdrop Kind="Base"/>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <!-- Title Bar -->
            <RowDefinition Height="Auto" />
            <!-- App Content -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid x:Name="TitleBar" VerticalAlignment="Top">
            <TextBlock x:Name="AppTitle" Text="CNT.USER" FontSize="15"
                       VerticalAlignment="Center" HorizontalAlignment="Left"
                       Margin="15, 0, 0, 0"/>
            <SelectorBar x:Name="ToolBar" SelectionChanged="SelectorBar2_SelectionChanged"
                         HorizontalAlignment="Center" VerticalAlignment="Center">
                <SelectorBarItem x:Name="SelectorBarItemRecent" Text="Home" Icon="Home" />
                <SelectorBarItem x:Name="SelectorBarItemShared" Text="Download" Icon="Download"/>
                <SelectorBarItem x:Name="SelectorBarItemSetting" Text="Setting" Icon="Setting"/>
                <SelectorBarItem x:Name="SelectorBarItemFavorites" Text="More" Icon="More" />
            </SelectorBar>
            
            
        </Grid>

        <!-- App content -->
        <Frame x:Name="rootFrame" Grid.Row="1"
            SourcePageType="Pages:Home"/>
    </Grid>
</Window>