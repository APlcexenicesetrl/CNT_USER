<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GUI.Pages.Home"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}">
            <Grid Padding="20">
                <InfoBar x:Name="ModuleOpeningStateInfo" 
                     IsOpen="True" IsClosable="False"
                     Severity="Warning"
                     Message="Module not started"/>

                <StackPanel VerticalAlignment="Bottom">
                    <Button Content="Run" Width="260" Margin="0, 0, 0, 5" Height="50"/>
                    <Button Content="Module And Plugine List" Width="260" Height="50"/>
                </StackPanel>
            </Grid>
        </Grid>
        <Grid Grid.Column="1">
            <Grid Height="200" VerticalAlignment="Top" Padding="20">
                <TextBlock Text="APlcexenicesetrl ©️ [Version 1.0.0.0]" FontSize="15" Foreground="#091417"/>
                <TextBlock Text="CNT USER for Windows" FontSize="25" FontWeight="Bold" Margin="0, 30, 0, 0" Foreground="#091417"/>
                <Line Stroke="#091417" X1="0" Y1="80" X2="100" Y2="80" StrokeThickness="2"/>
                <TextBlock Text="Install plugin modules for your computer." Margin="0, 100, 0, 0"/>

                <Grid.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="Yellow" Offset="0.0" />
                        <GradientStop Color="Red" Offset="1"/>
                    </LinearGradientBrush>
                </Grid.Background>
            </Grid>
        </Grid>
    </Grid>
</Page>
